import * as React from 'react';
import {Container, Row, Col} from 'react-bootstrap';

export const HomePage = ({}) => (
  <Container>
    <Row>
      <Col lg={12}>
        HomePage
      </Col>
    </Row>
  </Container>
);
